[package]
name = "requests-benchmark-native"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
futures-core = "0.3"
requests = { package = "requests-native", path = "../../crates/requests" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
